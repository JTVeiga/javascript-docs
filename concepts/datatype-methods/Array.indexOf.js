letters = ['a', 'b', 'c', 'd', 'e'];

index = letters.indexOf('b'); // returns 1, which is the index of 'b'
index2 = letters.indexOf('f'); // Returns -1, because there is no occurrence

console.log(index);
console.log(index2);
