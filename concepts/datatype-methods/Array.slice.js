letters = ['a', 'b', 'c', 'd', 'e'];

// index 0 to index 2
slice1 = letters.slice(0, 3); // a, b, c
slice2 = letters.slice(3);
slice3 = letters.slice(0, -1); // a b c d

console.log(slice1);
console.log(slice2);
console.log(slice3);
